import { Breakpoints } from "@suid/system/createTheme/createBreakpoints";
import StyledProps from "@suid/system/styledProps";
export interface Mixins {
    toolbar: StyledProps;
}
export default function createMixins(breakpoints: Breakpoints<any>, spacing?: any, mixins?: Record<string, StyledProps>): {
    toolbar: {
        [x: string]: number | {
            minHeight: number;
        };
        minHeight: number;
    };
};
//# sourceMappingURL=createMixins.d.ts.map