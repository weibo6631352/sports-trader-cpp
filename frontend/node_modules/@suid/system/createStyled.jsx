import { createDynamicComponent } from "./Dynamic";
import createStyle from "./createStyle";
import resolveStyledProps from "./resolveStyledProps";
import resolveSxProps from "./resolveSxProps";
import useTheme from "./useTheme";
import { randomString } from "@suid/utils";
import clsx from "clsx";
import { createMemo, splitProps, mergeProps } from "solid-js";
export function redefine(component, 
// eslint-disable-next-line @typescript-eslint/no-unused-vars
..._types) {
    return component;
}
export const skipProps = [
    "ownerState",
    "theme",
    "sx",
    "as",
];
function getStyleOverrides(name, theme) {
    return theme.components?.[name]?.styleOverrides;
}
function resolveStyles(useTheme, className, styles, inProps, options = {}) {
    return createMemo(() => {
        const theme = useTheme();
        const styleOverrides = getStyleOverrides(options.name, theme);
        const ownerState = inProps.ownerState;
        const baseStyles = styles.reduce((result, style) => {
            let styledProps;
            if (typeof style === "function") {
                styledProps = style({
                    ownerState,
                    theme,
                    get sx() {
                        return inProps.sx;
                    },
                    get as() {
                        return inProps.as;
                    },
                    props: inProps,
                });
            }
            else if (style) {
                styledProps = style;
            }
            if (styledProps)
                result.push(resolveStyledProps(styledProps, {
                    name: className,
                    __resolved: true,
                }));
            return result;
        }, []);
        let resolvedStyleOverrides = [];
        if (options.overridesResolver &&
            typeof options.overridesResolver === "function" &&
            styleOverrides) {
            resolvedStyleOverrides = options
                .overridesResolver(inProps, styleOverrides)
                .map((styleProp) => styleProp ? resolveStyledProps(styleProp, {}) : null)
                .filter(Boolean);
        }
        return baseStyles.concat(resolvedStyleOverrides);
    });
}
export function setSuidComponentType(cb, type) {
    cb["__suid"] = type;
}
function getSuidComponentType(input) {
    if (typeof input === "function")
        return input["__suid"];
}
/*@__NO_SIDE_EFFECTS__*/
function createStyled(config) {
    return function styled(Component, options = {}) {
        let cssClassName;
        let className;
        if (options.name) {
            const slot = options.slot || "Root";
            className = cssClassName = `${options.name}-${slot.slice(0, 1).toLowerCase() + slot.slice(1)}`;
        }
        else {
            className = `styled-${randomString()}`;
            cssClassName = "css";
        }
        const componentType = getSuidComponentType(Component);
        return function (...styles) {
            function StyledComponent(inProps) {
                let theme;
                const $useTheme = () => {
                    const inTheme = inProps.theme;
                    if (inTheme) {
                        return inTheme;
                    }
                    else if (theme) {
                        return theme;
                    }
                    else {
                        return (theme =
                            config && config.onUseTheme
                                ? config.onUseTheme()
                                : useTheme());
                    }
                };
                const [, otherProps] = splitProps(inProps, options.skipProps || skipProps);
                const inStyles = resolveStyles($useTheme, cssClassName, styles, inProps, options);
                const inSx = () => {
                    const sx = inProps.sx;
                    return (sx ? (Array.isArray(sx) ? sx : [sx]) : []);
                };
                const $component = () => {
                    if (componentType)
                        return Component;
                    const as = inProps.as;
                    return as ? as : Component;
                };
                const $componentType = componentType
                    ? () => componentType
                    : createMemo(() => getSuidComponentType($component()));
                const sx = options.skipSx
                    ? () => inStyles()
                    : () => {
                        const theme = $useTheme();
                        return [
                            ...inStyles(),
                            ...inSx().map((sx) => sx.__resolved ? sx : resolveSxProps(sx, theme)),
                        ];
                    };
                const styleClassName = createStyle(() => $componentType() ? undefined : sx());
                return createDynamicComponent($component, mergeProps(otherProps, {
                    get children() {
                        return inProps.children;
                    },
                    // [review] This property must be omitted on each component individually.
                    get component() {
                        return $componentType() ? inProps.component : null;
                    },
                    get as() {
                        return componentType ? inProps.as : undefined;
                    },
                    get sx() {
                        return $componentType() ? sx() : undefined;
                    },
                    get ownerState() {
                        return $componentType() === "system"
                            ? inProps.ownerState
                            : undefined;
                    },
                    get class() {
                        return clsx([
                            ...new Set([inProps.class, className, styleClassName()]),
                        ]);
                    },
                }));
            }
            setSuidComponentType(StyledComponent, "system");
            if (className)
                StyledComponent.toString = () => `.${className}`;
            return StyledComponent;
        };
    };
}
export default createStyled;
