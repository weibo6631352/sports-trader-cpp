import { mergeProps } from "solid-js";
export default function useProps(options) {
    const set = (v) => v;
    const inProps = mergeProps(() => options.propDefaults?.({
        set,
        inProps: options.props ?? inProps,
    }) ?? {}, options.props);
    return inProps;
}
