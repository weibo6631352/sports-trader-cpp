import { onCleanup, onMount } from "solid-js";
function onMountWithCleaning(effect) {
    let lastCleanup;
    onCleanup(() => lastCleanup?.());
    onMount(() => {
        lastCleanup = effect();
    });
}
export default onMountWithCleaning;
