import StyledEngineContext, { defaultStyledEngineContextValue, } from "./StyledEngineContext";
export default function StyledEngineProvider(inProps) {
    const value = {
        get cache() {
            return inProps.value?.cache ?? inProps.cache;
        },
        get injectFirst() {
            return inProps.value?.injectFirst ?? inProps.injectFirst;
        },
        get cleanupStyles() {
            return (inProps.value?.cleanupStyles ??
                inProps.cleanupStyles ??
                defaultStyledEngineContextValue.cleanupStyles);
        },
    };
    return (<StyledEngineContext.Provider value={value}>
      {inProps.children}
    </StyledEngineContext.Provider>);
}
