# tests/sim/CMakeLists.txt — sim target (mock harness 层, paper-mode 默认)
#
# Owner: 小宋  Sprint-2 W3 Wave 18
# 关联: skeleton v1 §1 / §2.3
#
# W3 占位: r12_sim 4 场景, mock harness header-only stub (W4+ 接小邹真 mock server)

add_subdirectory(r12_sim)
