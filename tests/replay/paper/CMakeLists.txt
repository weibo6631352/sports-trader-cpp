# tests/replay/paper/CMakeLists.txt — replay assertion headers (header-only)
# Owner: 小宋  E-04 replay v1
#
# 三个 assertion 头文件是 header-only library, 供 r11/ r20/ audit_chain/ 子目录 include.
# 无独立 binary target — 通过子目录测试 binary 验证.
